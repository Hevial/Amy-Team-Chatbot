[tool.pyright]
venvPath = "."
venv = ".venv"
extraPaths = [".", "src"]

[tool.ruff]
line-length = 100
target-version = "py311"
exclude = [
    ".agents",
    ".venv",
    ".git",
    "chroma_db",
    "**/__pyrefly_virtual__/**",
    "**/.gemini/**"
]
